{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default companion window permissions.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}

